// PreviewMode hook — host chrome for shareable preview reviews.
//
// Placeholder for Task C3a: the container in review_live.html.heex
// (#crit-preview-layout, phx-hook="PreviewMode") needs a registered hook to
// bind to. The full chrome — iframe pane, viewport bar, navigate/pin toggle,
// persistent comment side panel, and the postMessage bridge to the vendored
// preview-agent — lands in Task C3b. Keep the export name in sync with the
// hooks map in app.js.
export const PreviewMode = {
  mounted() {}
}
